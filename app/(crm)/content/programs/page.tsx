import PlaceholderPage from "../../_components/PlaceholderPage";

export default function ProgramsPage() {
  return (
    <PlaceholderPage
      section="Content"
      title="Programs"
      description="Recurring classes, halaqas, and study circles — with capacity, paid signup, and per-class RSVP lists."
      bullets={[
        "5-step wizard create — Basics → Schedule → Capacity → Image → Review",
        "Per-program detail page with RSVPs, notifications, and capacity tabs",
        "Reuse speakers from your registry, set max capacity, enable waitlist",
      ]}
    />
  );
}

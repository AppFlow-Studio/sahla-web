import PlaceholderPage from "../../_components/PlaceholderPage";

export default function SpeakersPage() {
  return (
    <PlaceholderPage
      section="People"
      title="Speakers"
      description="Build a registry of imams and guest scholars — once added, reuse them across Programs, Events, and Jummah."
      bullets={[
        "Photo, name, credentials, bio",
        "Reusable across Programs, Events, and Jummah scheduling",
        "Track how many programs each speaker has led",
      ]}
    />
  );
}
